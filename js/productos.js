/*creo la estructura de las cards que preciso para cada elemento.


function createCard(card){
    cards.forEach (card => {
        const nuevaCard = document.createElement('div');
       nuevaCard.classList = card.producto; });}
   
       const CONTENEDOR_TARJETAS = document.getElementById('div-Contenedor');
*/ 
